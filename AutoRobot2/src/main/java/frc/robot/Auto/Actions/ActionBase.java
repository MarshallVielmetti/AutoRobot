package frc.robot.Auto.Actions;

import edu.wpi.first.wpilibj2.command.Command;
import frc.robot.Auto.StartingPosition;

public abstract class ActionBase implements Command {
    // StartingPosition m_startingPosition; // Probably should get rid of this

    // public ActionBase(StartingPosition startingPosition) {
    // this.m_startingPosition = startingPosition;
    // }
}
